# ============================================
# Database Configuration
# ============================================
# PostgreSQL database credentials
POSTGRES_USER=taskflow
POSTGRES_PASSWORD=taskflow_secret
POSTGRES_DB=taskflow

# Database connection URL for backend
# Format: postgresql+asyncpg://user:password@host:port/database
# Note: Use 'db' as host when running in Docker Compose (service name)
#       Use 'localhost' when running backend locally outside Docker
DATABASE_URL=postgresql+asyncpg://taskflow:taskflow_secret@db:5432/taskflow

# ============================================
# Security & Authentication
# ============================================
# JWT secret key - MUST be changed in production!
# Generate a secure key: openssl rand -hex 32
JWT_SECRET_KEY=your-secret-key-change-in-production

# JWT algorithm (HS256, HS384, HS512, RS256, etc.)
JWT_ALGORITHM=HS256

# Access token expiration time in minutes
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ============================================
# Backend Configuration
# ============================================
# CORS allowed origins (comma-separated list)
# Include all frontend URLs that need to access the API
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Backend server port (used in docker-compose.yml)
BACKEND_PORT=8000

# ============================================
# Frontend Configuration
# ============================================
# API URL that the frontend will use to connect to backend
# This is used by the Next.js client-side code
# Note: Use http://localhost:8000 when running locally
#       Use your production API URL in production
NEXT_PUBLIC_API_URL=http://localhost:8000

# Frontend server port (used in docker-compose.yml)
FRONTEND_PORT=3000

# ============================================
# Docker Configuration
# ============================================
# PostgreSQL exposed port on host machine
# Using 5433 to avoid conflicts with local PostgreSQL installations
POSTGRES_PORT=5433

# ============================================
# Development Settings (Optional)
# ============================================
# Enable debug mode (true/false)
DEBUG=true

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# ============================================
# Production Notes
# ============================================
# When deploying to production:
# 1. Generate a strong JWT_SECRET_KEY
# 2. Use strong POSTGRES_PASSWORD
# 3. Update BACKEND_CORS_ORIGINS with your production domain
# 4. Set NEXT_PUBLIC_API_URL to your production API URL
# 5. Set DEBUG=false
# 6. Consider using environment-specific .env files (.env.production)
